<div class="container" style="margin-top: 20px;margin-bottom: 20px;">
  <div class="left">
    <%= form_tag( notes_path, :method => "get", id: "search-form") do %>
        <%=text_field_tag :search,params[:search], placeholder: "Search Stricky..." ,:style=>"padding:3px;margin-right:1px;width:200px;height:29px;"%>
        <%= submit_tag 'SEARCH',:author=>nil,:class=>"btn btn-custom"%>
    <% end %>
  </div>
  <div class="right">
    <%= link_to 'NEW STICKY', new_note_path, :class=>"btn btn-custom pull-right" , :style=>"margin-left:10px;"%>
  </div>
</div>
<div class="container">
  <div class="form-group" style="min-height: 500px;">
    <table class="table table-responsive">
      <thead>
      <tr>
        <th>AUTHOR</th>
        <th>CATEGORY</th>
        <th>TIMESTAMP</th>
        <th>HIDE STICKY</th>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @notes.each do |note| %>
          <tr>
            <td><%=link_to note.author,note %></td>
            <td><%= note.category %></td>
            <td><%= note.timestamp %></td>
            <td><%= note.hide_sticky %></td>
            <td><%= link_to 'Show', note %></td>
            <td><%= link_to 'Edit', edit_note_path(note) %></td>
            <td><%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>