<%= simple_form_for(@tag) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :uuid %>
    <%= f.input :tag_name %>
    <%= f.input :tag_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>